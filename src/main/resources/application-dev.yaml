server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/Community
    username: postgres  # Your Render username
    password: nani  # Your Render password
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect

  mail:
    host: smtp.gmail.com
    port: 587
    username: sudhrshan18@gmail.com
    password: cstzzwvoxzyghcsg
    protocol: smtp
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
          ssl:
            trust: smtp.gmail.com

  thymeleaf:
    prefix: classpath:/templates/
    suffix: .html
    mode: HTML
    encoding: UTF-8
    cache: false

app:
  email:
    from: sudhrshan18@gmail.com
  mail:
    mailtrap:
      token: bc4156e080801c12749c199104a8d755

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,prometheus

  endpoint:
    prometheus:
      enabled: true

  metrics:
    export:
      prometheus:
        enabled: true
